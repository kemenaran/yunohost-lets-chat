var ldap = require('ldapjs');

var client = ldap.createClient({url: 'ldap://localhost:9999'});

client.on('error', function(err) {
  console.log(err);
});

client.search('ou=people', {filter: '(uid=user)'}, function(err, res) {
  console.log(err, res);
});
